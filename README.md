# pacman-manyrepos-totallybroken

Add as many repos as possible to find good stuf with `pacman -Fx`
and when lazy to bild from source, for example: `chroot . pacman -Spdd firefox-wayland-hg | pacman -S -`
